export interface Product {
  id: number;
  name: string;
  price: number;
  image: string;
  stock: number;
  description: string;
}

export interface Carts {
  id: number;
  name: string;
  price: number;
  image: string;
  qty: number;
  description: string;
}
